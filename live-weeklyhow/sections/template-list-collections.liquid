<div class="flex flex-col gap-y-10 my-10">
    <div class="grid md:grid-cols-3 lg:grid-cols-4 gap-4">
        {% for collection in collections %}
            {% render 'collection-card', collection: collection, index: forloop.index %}
        {% endfor %}
    </div>
</div>